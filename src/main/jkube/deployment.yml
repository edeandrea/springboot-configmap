metadata:
  labels:
    app.openshift.io/runtime: spring-boot
    app.kubernetes.io/part-of: poc
spec:
  template:
    spec:
      containers:
        - env:
            - name: JAVA_DEBUG
              value: "true"
